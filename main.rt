let gateway = {
  name = traefik-gateway
  namespace = kube-system
}

let entrypoint = websecure

route git.lucalise.ca -> cgit:22 {
  name = cgit + 2
  namespace = git
}

route tcp gitolite:22 {
  name = gitolite
  namespace = git
  entrypoint = ssh
}
