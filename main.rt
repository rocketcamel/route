let gateway = {
  name = traefik-gateway,
  namespace = kube-system
}

let entrypoint = websecure
let private_middleware_name = private-networks

route git.lucalise.ca -> cgit:22 {
  name = cgit
  namespace = git
}

route tcp gitolite:22 {
  name = gitolite
  namespace = git
  entrypoint = ssh
}
